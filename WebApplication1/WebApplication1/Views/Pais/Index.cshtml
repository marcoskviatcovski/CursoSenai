@model IEnumerable<WebApplication1.Models.Pais>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.pais1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sigla)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.pais1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sigla)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_pais }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_pais }) |
            @Html.ActionLink("Excluir", "Excluir", new { id=item.id_pais })
        </td>
    </tr>
}

</table>
